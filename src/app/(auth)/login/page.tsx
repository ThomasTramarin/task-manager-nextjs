import LoginForm from "../../../components/auth/LoginForm";

export default function RegisterPage() {
  return (
    <main className="bg-background-1 p-4 min-h-screen flex items-center justify-center">
      <LoginForm />
    </main>
  );
}
